using System;
public enum Days {
    Monday, 
    Tuesday, 
    Wednesday, 
    Thursday, 
    Friday
}
class MyEnumDemo1 {
    static void Main() {
        // value typed category
        Days d = 0;
        Console.WriteLine(d);
  }
}

